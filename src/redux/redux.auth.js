
const initState = {
    authUser:false,// 没有用户登录信息 默认:false
}

//reducer -> auth
export function auth(state = initState,action) {
    switch(action.type){

        default:
            return state
    }
}